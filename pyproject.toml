[project]
name = "aia-backend"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=1.0.0",
    "pytest-cov>=6.0.0",
    "pytest-mock>=3.15.0",
    "tenacity>=9.0.0",
    "ruff>=0.11.0",
    "pre-commit>=4.0.0",
    "detect-secrets>=1.5.0",
]
